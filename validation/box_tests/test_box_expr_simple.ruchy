// Test Box<Expr> in enum
enum Expr {
    Number(String),
    Add(Box<Expr>, Box<Expr>)
}

fun main() {
    let one = Expr::Number("1".to_string());
    let two = Expr::Number("2".to_string());
    let add = Expr::Add(Box::new(one), Box::new(two));

    println("✅ Box<Expr> in enum works!");
}

main();
