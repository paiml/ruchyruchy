[package]
name = "ruchyruchy"
version = "0.2.0"
edition = "2021"
authors = ["RuchyRuchy Development Team"]
description = "Educational bootstrap compiler with EXTREME TDD (100% complete) - debugging tools for Ruchy language"
repository = "https://github.com/paiml/ruchyruchy"
license = "MIT"
keywords = ["ruchy", "compiler", "bootstrap", "debugging", "education"]
categories = ["development-tools", "compilers"]
readme = "README.md"

[lib]
name = "ruchyruchy"
path = "src/lib.rs"

[[bin]]
name = "ruchydbg"
path = "src/bin/ruchydbg.rs"

[dependencies]
# Currently minimal - add dependencies as needed

[dev-dependencies]
# Test dependencies

[features]
default = []
# Feature flags for optional functionality
bootstrap = []
debugging = []

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
