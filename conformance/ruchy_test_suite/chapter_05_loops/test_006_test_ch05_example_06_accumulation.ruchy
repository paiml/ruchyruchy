// Test: test_ch05_example_06_accumulation
// Chapter: 05 - loops
// Description: Test case from chapter 5 - loops
//
// Expected Output:

fun calculate_sum(count: i32) -> i32 {
    let mut sum = 0;
    let mut i = 1;
    while i <= count {
        sum = sum + i;
        i = i + 1;
    }
    sum
}

fun calculate_average(count: i32) -> f64 {
    let sum = calculate_sum(count);
    let average = (sum as f64) / (count as f64);
    average
}

fun main() {
    let sum = calculate_sum(10);
    let avg = calculate_average(10);
    println("Sum of 1..10: {}", sum);
    println("Average: {}", avg);
}
