// Test exact syntax from BOUNDARIES.md
enum LLVMType {
    I32,
    Pointer(Box<LLVMType>),
    Array(Box<LLVMType>, i32)
}

fun main() {
    let inner = LLVMType::I32;
    let ptr = LLVMType::Pointer(Box::new(inner));
    println("Box in enum works!");
}

main();
